# renderman work remaining

- TODO: Run renders and writes in parallel to speed up the process.
- TODO: Add a `--dry-run` option to preview the renders without writing them.
- TODO: Add a `diff` command to show the differences between the rendered manifests and the current manifests in the cluster.
- TODO: Make `releases` part of the `app` config obj
- TODO: Source types values and "literals" available from a registry of renderers by source type 
# TODO: make renderman config file an api kind

